services:
  # ── Infrastructure ──────────────────────────────────────────
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: vibeful
      POSTGRES_PASSWORD: vibeful_dev
      POSTGRES_DB: vibeful
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vibeful"]
      interval: 3s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      retries: 5

  # ── gRPC Bridge ─────────────────────────────────────────────
  envoy:
    image: envoyproxy/envoy:v1.30-latest
    ports:
      - "8080:8080"
    volumes:
      - ./envoy.yaml:/etc/envoy/envoy.yaml:ro
    depends_on:
      agent-engine:
        condition: service_healthy

  # ── Agent Engine (Python, LangGraph, REST + gRPC) ───────────
  agent-engine:
    build:
      context: .
      dockerfile: packages/agent-engine/Dockerfile
    ports:
      - "50051:50051"   # gRPC (SDK streaming)
      - "50052:50052"   # REST (management console)
    environment:
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      DATABASE_URL: postgresql://vibeful:vibeful_dev@postgres:5432/vibeful
      REDIS_URL: redis://redis:6379
      GRPC_PORT: "50051"
      REST_PORT: "50052"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python3 -c 'import urllib.request; urllib.request.urlopen(\"http://localhost:50052/health\")'"]
      interval: 5s
      retries: 5

  # ── Management Console (React/Vite) ─────────────────────────
  management-console:
    build:
      context: .
      dockerfile: packages/management-console/Dockerfile
    ports:
      - "5174:5174"
    environment:
      VITE_API_URL: http://agent-engine:50052
    depends_on:
      agent-engine:
        condition: service_healthy

  # ── MCP Servers (Node/TS) ───────────────────────────────────
  mcp-web-search:
    build:
      context: .
      dockerfile: packages/mcp-servers/Dockerfile
    ports:
      - "3100:3100"
    environment:
      MCP_PORT: "3100"
      MCP_SERVICE: web-search

  mcp-file-read:
    build:
      context: .
      dockerfile: packages/mcp-servers/Dockerfile
    ports:
      - "3101:3101"
    environment:
      MCP_PORT: "3101"
      MCP_SERVICE: file-read
    volumes:
      - .:/workspace:ro

  mcp-calculator:
    build:
      context: .
      dockerfile: packages/mcp-servers/Dockerfile
    ports:
      - "3102:3102"
    environment:
      MCP_PORT: "3102"
      MCP_SERVICE: calculator

  # ── Proxy (FastAPI, stateless) ─────────────────────────────
  proxy:
    build:
      context: .
      dockerfile: packages/proxy/Dockerfile
    ports:
      - "8000:8000"
    environment:
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      AGENT_ENGINE_URL: agent-engine:50051
      DATABASE_URL: postgresql://vibeful:vibeful_dev@postgres:5432/vibeful
    depends_on:
      postgres:
        condition: service_healthy
      agent-engine:
        condition: service_healthy

  # ── SDK Dev Server (React/Vite) ────────────────────────────
  sdk:
    build:
      context: .
      dockerfile: packages/sdk/Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - ./packages/sdk/src:/app/packages/sdk/src:ro
    environment:
      VITE_ENVOY_URL: http://localhost:8080

volumes:
  pgdata:
